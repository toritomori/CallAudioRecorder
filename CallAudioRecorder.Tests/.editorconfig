# Дополняет корневой .editorconfig для тестов.
[*.cs]
# CA1707 — «без подчёркиваний в именах». Для тестов имя вида Метод_Сценарий_Результат —
# принятая конвенция xUnit: по нему в отчёте видно, что именно сломалось.
dotnet_diagnostic.CA1707.severity = none
